export interface ProductsDTO {
    isChecked: boolean,
    name: String,
    category: String,
    brand: String, 
    price: Number, 
    formatSize: String,
    marked: String,
    image: String,
    units?: Number,
};